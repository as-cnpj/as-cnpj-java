# Tests

The Java runtime uses its own test runner without external dependencies.

Current coverage:

- normalization;
- formatting;
- strict mode;
- check digits;
- batch validation;
- shared vectors;
- length, ASCII, and batch guards.

