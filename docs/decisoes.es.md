# Decisiones

## Bootstrap sin Maven

Esta semilla usa `javac/java` porque el entorno local actual no tiene `mvn`.

## Java 17

Java 17 es la base local por estar disponible y ser LTS.

## Sin dependencias externas de runtime

El nucleo y el runner de pruebas no dependen de bibliotecas de terceros.

