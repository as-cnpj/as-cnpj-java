# Changelog

## 0.1.0

- primeira seed local do runtime Java;
- validacao, normalizacao, formatacao e calculo de DV para CNPJ numerico e alfanumerico;
- validacao em lote com `items` e `summary`;
- testes locais com `javac/java` e vetores compartilhados.

